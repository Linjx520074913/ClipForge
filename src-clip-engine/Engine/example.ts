// import { ClipEngine } from './ClipEngine';

// const engine = await ClipEngine.create();

// const videoTrack_0 = new VideoTrack('video-track-0', engine.getContext(), canvasRef.value);
// engine.addTrack(videoTrack_0);

// await engine.render(videoFrame);